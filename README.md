# cropSVG
Small helper to crop SVGs to perfect square and to compile a TSX React component

## Install
clone repo
`cd cropSVG`

## Start
`node server.js` (Choose Option 1 or 2)
`npm run dev` (Starts Frontend, will have to adjust filenames and imports though)
